cert-manager-webhook-infoblox has been deployed.

Solver name : infoblox
Group name  : {{ .Values.groupName }}

To use this webhook in a ClusterIssuer, add the following stanza to your
issuer's solver configuration:

  solvers:
    - dns01:
        webhook:
          groupName: {{ .Values.groupName }}
          solverName: infoblox
          config:
            host: <infoblox-host>
            view: <dns-view>
            usernameSecretRef:
              name: infoblox-credentials
              key: username
            passwordSecretRef:
              name: infoblox-credentials
              key: password

For full documentation see:
  https://github.com/tazthemaniac/cert-manager-webhook-infoblox
